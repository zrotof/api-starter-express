const router = require('express').Router();

//import route in the same folder
//example : const exampleRoutes = require('./example/exampleRoutes');

//router.use("/exampleq", exampleRoutes);

module.exports = router;